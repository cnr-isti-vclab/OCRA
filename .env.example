## Example environment for local development with Vite.
## Copy this file to `.env` and adjust values if needed.
## These variables are only used by `npm run dev` (Vite). Docker uses runtime env via config.js.

VITE_PROVIDER_URL=http://localhost:8081
VITE_REALM=demo
VITE_CLIENT_ID=react-oauth
VITE_ISSUER=http://localhost:8081/realms/demo
VITE_REDIRECT_URI=http://localhost:5173
VITE_SCOPE=openid profile email

# Database connection for Prisma (development)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/oauth_demo"
